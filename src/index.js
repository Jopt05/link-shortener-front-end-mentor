import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { LinkShortenerApp } from './LinkShortenerApp'

ReactDOM.render(
  <LinkShortenerApp />,
  document.getElementById('root')
);
